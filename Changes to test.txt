<aura:component implements="force:appHostable,flexipage:availableForAllPageTypes,flexipage:availableForRecordHome,force:hasRecordId,forceCommunity:availableForAllPageTypes,force:lightningQuickActionWithoutHeader" access="global">
    <aura:attribute name="showConfirmDialogBox" type="boolean" default="false"/>

    <lightning:workspaceAPI aura:id="workspace" />
    <lightning:navigation aura:id="navService"/>
    <c:scnewcase aura:id="ncase" onalerteventcase="{!c.closeModal}" oncreatecaseevents="{!c.openCaseEdit}" />

    <aura:handler name="init" value="{!this}" action="{!c.init}"/>

    <!-- Main dialog container -->
    <div aura:id="toggleDiv" class="slds-modal slds-fade-in-open">
        <!-- Dialog container with adjusted size -->
        <div class="slds-modal__container" style="width: 20rem; max-height: 12rem; overflow: hidden;">
            <!-- Conditional content -->
            <aura:if isTrue="{!v.showConfirmDialogBox}">
                <!-- Spinner and progress message -->
                <div class="slds-align_absolute-center" style="text-align: center; padding: 1rem;">
                    <lightning:spinner alternativeText="Loading" size="small" />
                    <p style="margin-top: 1rem; font-weight: bold; color: #000;">Case creation is in progress. You will be redirected to the Case record page.</p>
                </div>
                <aura:set attribute="else">
                    <!-- Confirmation message with Yes/No buttons -->
                    <div class="slds-align_absolute-center slds-modal__content" style="padding-top: 1rem; text-align: center;">
                        <b>Are you sure you want to open a Case?</b>
                    </div>
                    <div style="text-align: center; margin-top: 1rem;">
                        <lightning:button name="Cancel" label="No" onclick="{!c.handleConfirmDialogCancel}" class="slds-m-right_small"/>
                        <lightning:button variant="brand" name="Ok" label="Yes" onclick="{!c.handleConfirmDialogOk}"/>
                    </div>
                </aura:set>
            </aura:if>
        </div>
    </div>
</aura:component>
